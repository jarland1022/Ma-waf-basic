# 复制为 .env 后按需修改：  copy .env.example .env
# 数据平面（防护入口）
WAF_HTTP_PORT=8080

# 社区控制台（含专业版介绍与试用引导）
CONSOLE_PORT=8090

# On = 拦截；DetectionOnly = 只记录不拦截（上线初期建议先观察）
MODSEC_RULE_ENGINE=On

# 专业版试用入口不在本文件。发布前请修改 console/config.js 里的 contactHref。

# OWASP CRS 规则占位目录
# 请将 CRS 4.x 的 rules/*.conf 放到此目录，或由部署脚本软链：
#   ln -s /usr/local/src/coreruleset-4.x/rules /usr/local/nginx/conf/modsecurity/rules
#
# 若暂时没有 CRS，保留本文件即可（避免 include *.conf 匹配为空时报错）

# 空操作占位（不产生告警）
SecAction "id:109999,phase:1,pass,nolog"

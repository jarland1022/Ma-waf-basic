# OpenAPI 基线规则占位（由 openapi_guard.py gen-rules 生成后替换）
# META: enabled=false; priority=50; cve=openapi-baseline
#
# 生成示例:
#   python3 tools/openapi_guard.py gen-rules \
#     --spec /usr/local/ma-waf/conf/openapi.yaml \
#     --out /usr/local/nginx/conf/modsecurity/custom/130000-openapi.conf
#
# 默认禁用：避免无规范时误拦。生成并评审后将 META.enabled 改为 true。

# SecRule REQUEST_URI "@rx ^/api/" \
#     "id:130001,phase:1,pass,nolog,\
#     msg:'openapi placeholder - replace with gen-rules output',\
#     tag:'ma-waf/openapi'"
